# CHANGELOG

sleek-pdf 프로젝트의 주요 수정 사항을 날짜별로 기록합니다. 최신 날짜가 위로 오도록 추가합니다.
각 항목은 "무슨 문제였는지 → 어떻게 고쳤는지"를 간단히 남깁니다.

## 2026-09-22

### 수정
- **스탬프/텍스트/워터마크 오버레이의 위치·크기가 캔버스 미리보기와 다운로드한 PDF에서 다르게 나오던 문제**
  - 문제 1: 위치 프리셋(예: 스탬프 기본값 `bottom-right`)이 캔버스 미리보기에서는 반영되지 않고 드래그 전까지 항상 화면 정중앙에 그려짐. 실제 PDF에는 프리셋 위치대로 찍혀서 서로 달랐음.
  - 문제 2: PDF에 텍스트를 그릴 때 baseline 위치를 `fontSize`로 단순 근사해서, 드래그로 정확히 옮겨놔도 폰트의 실제 ascent/descent 비율과 안 맞아 세로 위치가 미묘하게 어긋남.
  - 해결: 미리보기에 위치 프리셋용 앵커 계산을 추가해 PDF의 위치 계산 규칙과 동일하게 맞춤. PDF baseline 계산은 pdf-lib이 제공하는 실제 폰트 메트릭(`font.heightAtSize`)을 사용하도록 교체. (Node 스크립트로 검증: 중심 오차가 약 15pt → 0.5pt로 감소)
  - 관련 파일: `src/components/OverlayPreviewLayer.jsx`, `src/utils/pdfHelpers.js`

- **스탬프 텍스트가 미리보기와 PDF에서 아예 다른 폰트처럼 보이던 문제, 텍스트가 볼드일 때 PDF에서 살짝 오른쪽으로 쏠리던 문제**
  - 문제 1: 스탬프는 미리보기에서 `font-weight: bold`(가짜 볼드)와 `letter-spacing: 0.1em`을 항상 쓰는데, PDF에는 이 두 스타일이 전혀 반영되지 않아 폰트 파일은 같아도 완전히 다른 모양으로 보였음. (실측 확인: 미리보기가 실제로 쓰는 폰트와 PDF가 embed하는 malgun.ttf는 렌더링 폭이 100% 동일함 — 폰트 파일 자체의 문제는 아니었음)
  - 문제 2: 텍스트/워터마크의 볼드 시뮬레이션이 원본 위치에서 오른쪽으로만 겹쳐 그리는 방식이라, 잉크의 무게중심이 오른쪽으로 쏠려 있었음.
  - 해결: 스탬프를 글자 단위로 나눠서 자간(0.1em)을 두고, 글자마다 좌우 대칭으로 겹쳐 그려 볼드를 구현. 텍스트/워터마크의 볼드 시뮬레이션도 좌우 대칭 오프셋으로 수정.
  - 관련 파일: `src/utils/pdfHelpers.js`

## 2026-09-21

### 수정
- **그리드 모드에서 페이지 추가(+) 버튼이 행 중앙이 아니라 캔버스 전체 중앙에 뜨던 문제**
  - 문제: 그리드 컨테이너(`.page-grid--grid`)에 `align-content`가 지정되지 않아, 행이 1개뿐일 때(예: 페이지가 적어 한 줄만 채워졌을 때) 남는 세로 공간이 그 한 행 자체를 늘려버림.
  - 해결: `align-content: start`를 추가해 행이 자기 높이만 차지하고, 남는 공간은 마지막 행 아래로만 남도록 함.
  - 관련 파일: `src/components/PageGrid.css`

---

앞으로 이 프로젝트를 수정할 때마다 위와 같은 형식으로 새 날짜 항목을 맨 위에 추가해 기록을 남깁니다.
